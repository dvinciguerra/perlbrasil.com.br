{
    # server configurations
    hypnotoad => {
        proxy   => 1,
        listen  => ['http://*:5012'],
        workers => 4,
        clients => 2,

        graceful_timeout   => 2,
        heartbeat_timeout  => 600,
        inactivity_timeout => 600,
    },
}
